# Training / evaluation (already present in your env, pinned loosely)
torch>=2.2
torchvision>=0.17
timm>=1.0
scikit-learn>=1.3
pillow>=10.0
numpy>=1.26
matplotlib>=3.7
tqdm>=4.66

# Export + on-device inference (install for the Pi step)
onnx>=1.16
onnxruntime>=1.17

# Stage-1 detector for the full-frame pipeline (deployment only)
# PytorchWildlife>=1.2   # uncomment / install when wiring up MegaDetector
